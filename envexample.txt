# ============================================================================
#  CONFIGURAÇÃO CENTRAL (RAIZ) — Atendimento WhatsApp com IA (NestJS)
#  Copie para `.env` na RAIZ do repositório. Orquestra docker-compose, API e worker.
#  O sistema sobe 100% SIMULADO sem nenhuma credencial.
# ============================================================================

# ─── Modo dos provedores (HÍBRIDO E INDEPENDENTE) ─────────────────────────
# Cada integração é escolhida separadamente. Combine como quiser:
#   OPENAI real + META simulada  | OPENAI simulada + META real | ambos, ou nenhum.
#   auto       -> usa real se houver credencial válida, senão cai para simulado
#   real       -> força a integração real (exige credenciais)
#   simulated  -> força o stub local (não chama rede externa)
OPENAI_MODE=auto
META_MODE=simulated

# ─── OpenAI ───────────────────────────────────────────────
# Cole sua chave real para usar a IA de verdade (OPENAI_MODE=auto detecta sozinho).
OPENAI_API_KEY=sk-proj-troque-pela-sua-chave
OPENAI_MODEL=gpt-4o-mini

# ─── Meta WhatsApp Cloud API ──────────────────────────────
# Token de verificação do handshake do webhook (você define; use o mesmo no painel da Meta).
META_VERIFY_TOKEN=meu-verify-token-secreto
# App Secret — usado para validar a assinatura X-Hub-Signature-256.
# Com o mock, mantenha IGUAL ao META_APP_SECRET do docker-compose.yml.
META_APP_SECRET=super-secret-app-secret-trocar
# Token de acesso para enviar mensagens. Com o mock, qualquer valor serve.
META_TOKEN=mock-token
# Base da API da Meta. Mock: http://localhost:8001  |  Real: https://graph.facebook.com/v21.0
META_API_BASE_URL=http://localhost:8001
# ID do número de telefone (origem dos envios). Mock aceita qualquer valor.
META_PHONE_NUMBER_ID=123456789012345

# ─── Banco de dados (Postgres + Drizzle) ──────────────────
DATABASE_URL=postgres://postgres:postgres@localhost:5432/atendimento

# ─── Fila (AWS SQS via LocalStack) ────────────────────────
AWS_REGION=us-east-1
AWS_ENDPOINT_URL=http://localhost:4566
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
SQS_JOBS_QUEUE_URL=http://localhost:4566/000000000000/atendimento-jobs
SQS_JOBS_DLQ_URL=http://localhost:4566/000000000000/atendimento-jobs-dlq
SQS_AUDIT_QUEUE_URL=http://localhost:4566/000000000000/atendimento-audit
SQS_AUDIT_DLQ_URL=http://localhost:4566/000000000000/atendimento-audit-dlq
# Quantas vezes uma mensagem é reentregue antes de ir para a DLQ (redrive policy).
SQS_MAX_RECEIVE_COUNT=3
# Visibilidade (segundos) enquanto uma mensagem está sendo processada.
SQS_VISIBILITY_TIMEOUT=30

# Redis continua no docker-compose mas NÃO é usado (optamos por SQS).
REDIS_URL=redis://localhost:6379

# ─── Base de conhecimento (RAG) ───────────────────────────
KNOWLEDGE_BASE_DIR=../knowledge-base

# ─── Multi-tenant ─────────────────────────────────────────
# Tenant default semeado no banco; o webhook resolve o tenant pelo phone_number_id.
DEFAULT_TENANT_ID=11111111-1111-1111-1111-111111111111
DEFAULT_TENANT_NAME=Myde Telecom

# ─── Function calling (bônus): endpoint mock de status de pedido ──
# Vazio = usa o próprio endpoint interno GET /internal/orders/:protocol da API.
ORDER_STATUS_BASE_URL=

# ─── App ──────────────────────────────────────────────────
PORT=8000
LOG_LEVEL=info
# Porta publicada no HOST pelo docker-compose (a interna é sempre 8000).
# Mude se a 8000 estiver ocupada na sua máquina (ex.: API_HOST_PORT=8080).
API_HOST_PORT=8000

# ─── Rate limit (NestJS Throttler) ────────────────────────
# Janela em milissegundos e máximo de requisições por janela/ IP.
THROTTLE_TTL=60000
THROTTLE_LIMIT=100
